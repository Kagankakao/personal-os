<Window x:Class="KeganOS.Views.AddManualTimeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Manual Time" 
        Height="350" Width="400"
        WindowStartupLocation="CenterOwner"
        Background="#1A1A1A"
        FontFamily="Consolas"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True">
    
    <Window.Resources>
        <Style x:Key="InputLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
        
        <Style x:Key="SpinnerTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Width" Value="60"/>
        </Style>
    </Window.Resources>
    
    <Border BorderBrush="#444444" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Title Bar -->
            <Grid Grid.Row="0" Background="#0D0D0D" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <TextBlock Text="⏱️ Add Manual Time" Foreground="#CCCCCC" VerticalAlignment="Center" Margin="15,0,0,0" FontSize="14"/>
                <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Center" 
                        Width="35" Height="35" Background="Transparent" BorderThickness="0"
                        Foreground="#888888" Click="CloseButton_Click" Cursor="Hand"/>
            </Grid>
            
            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="20,15,20,10">
                
                <!-- Date -->
                <TextBlock Text="📅 Date" Style="{StaticResource InputLabel}"/>
                <DatePicker x:Name="DateInput" SelectedDate="{x:Static sys:DateTime.Today}"
                            xmlns:sys="clr-namespace:System;assembly=mscorlib"
                            Background="#252525" Foreground="#CCCCCC" BorderBrush="#444444"
                            Margin="0,0,0,15"/>
                
                <!-- Duration -->
                <TextBlock Text="⏰ Duration" Style="{StaticResource InputLabel}"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBox x:Name="HoursInput" Style="{StaticResource SpinnerTextBox}" Text="0"/>
                    <TextBlock Text="h" Foreground="#888888" VerticalAlignment="Center" Margin="5,0,15,0"/>
                    
                    <TextBox x:Name="MinutesInput" Style="{StaticResource SpinnerTextBox}" Text="25"/>
                    <TextBlock Text="m" Foreground="#888888" VerticalAlignment="Center" Margin="5,0,15,0"/>
                    
                    <TextBox x:Name="SecondsInput" Style="{StaticResource SpinnerTextBox}" Text="0"/>
                    <TextBlock Text="s" Foreground="#888888" VerticalAlignment="Center" Margin="5,0,0,0"/>
                </StackPanel>
                
                <!-- Note -->
                <TextBlock Text="📝 Note (optional)" Style="{StaticResource InputLabel}"/>
                <TextBox x:Name="NoteInput" Background="#252525" Foreground="#CCCCCC" 
                         BorderBrush="#444444" Height="60" TextWrapping="Wrap"
                         AcceptsReturn="True" MaxLength="500" Margin="0,0,0,15"/>
                
                <!-- Update Pixe.la -->
                <StackPanel Orientation="Horizontal">
                    <CheckBox x:Name="UpdatePixelaCheckBox" IsChecked="True" VerticalAlignment="Center"/>
                    <TextBlock Text=" Update Pixe.la graph" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                </StackPanel>
                
            </StackPanel>
            
            <!-- Footer Buttons -->
            <Border Grid.Row="2" BorderBrush="#333333" BorderThickness="0,1,0,0" Padding="20,15">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Padding="15,8" Background="#333333" Foreground="#CCCCCC"
                            BorderBrush="#555555" Click="CancelButton_Click" Cursor="Hand"/>
                    <Button Content="Add Time" Padding="15,8" Background="#2D5A27" Foreground="#CCCCCC"
                            BorderBrush="#3D7A37" Margin="10,0,0,0" Click="AddTimeButton_Click" Cursor="Hand"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
