<Window x:Class="KeganOS.Views.ProfileSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="KeganOS" 
        Height="500" Width="450"
        WindowStartupLocation="CenterScreen"
        Background="#0D0D0D"
        FontFamily="Consolas"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True">
    
    <Window.Resources>
        <Style x:Key="ProfileCard" TargetType="Border">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>
    
    <Border BorderBrush="#333333" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Title Bar -->
            <Grid Grid.Row="0" Background="#0D0D0D" MouseDown="TitleBar_MouseDown">
                <TextBlock Text="KeganOS" Foreground="#666666" FontSize="12" 
                           Margin="15,10" VerticalAlignment="Center"/>
                <Button Content="✕" HorizontalAlignment="Right" 
                        Background="Transparent" Foreground="#666666"
                        BorderThickness="0" Padding="15,10" Cursor="Hand"
                        Click="CloseButton_Click"/>
            </Grid>
            
            <!-- Header -->
            <StackPanel Grid.Row="1" Margin="30,20,30,20">
                <TextBlock Text="Welcome back." Foreground="#FFFFFF" FontSize="24"/>
                <TextBlock Text="Select your profile to continue." Foreground="#666666" FontSize="12" Margin="0,5,0,0"/>
            </StackPanel>
            
            <!-- Profile List -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="30,0">
                <ItemsControl x:Name="ProfileList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource ProfileCard}" 
                                    MouseDown="ProfileCard_Click" Tag="{Binding Id}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Text="👤" FontSize="24" VerticalAlignment="Center"/>
                                    
                                    <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding DisplayName}" Foreground="#FFFFFF" FontSize="14"/>
                                        <TextBlock Text="{Binding DisplaySymbol}" Foreground="#555555" FontSize="11"/>
                                    </StackPanel>
                                    
                                    <TextBlock Grid.Column="2" Text="→" Foreground="#444444" 
                                               FontSize="18" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            
            <!-- Create New Profile -->
            <Border Grid.Row="3" Margin="30,10,30,10" Padding="15" 
                    Background="Transparent" BorderBrush="#333333" BorderThickness="1" 
                    Cursor="Hand" MouseDown="CreateProfile_Click">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="+" Foreground="#555555" FontSize="18" Margin="0,0,10,0"/>
                    <TextBlock Text="Create New Profile" Foreground="#555555" FontSize="13" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
            
            <!-- Footer -->
            <TextBlock Grid.Row="4" Text="Your Personal Life Operating System" 
                       Foreground="#333333" FontSize="10" 
                       HorizontalAlignment="Center" Margin="0,10,0,20"/>
        </Grid>
    </Border>
</Window>
