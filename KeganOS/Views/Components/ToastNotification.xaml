<UserControl x:Class="KeganOS.Views.Components.ToastNotification"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="80" Width="300">
    <UserControl.Resources>
        <Storyboard x:Key="SlideIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
            <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,20,0,0" To="0,0,0,20" Duration="0:0:0.3" DecelerationRatio="0.9"/>
        </Storyboard>
        <Storyboard x:Key="FadeOut">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.5" BeginTime="0:0:4"/>
        </Storyboard>
    </UserControl.Resources>
    
    <Border Background="#151515" BorderBrush="#FFCC00" BorderThickness="1" Padding="15,10"
            Cursor="Hand" MouseLeftButtonDown="Toast_Click">
        <StackPanel>
            <Grid Margin="0,0,0,5">
                <TextBlock Text="[!] ACHIEVEMENT UNLOCKED" Foreground="#FFCC00" FontSize="10" FontWeight="Bold"/>
                <TextBlock Text="✖" Foreground="#444444" FontSize="10" HorizontalAlignment="Right" VerticalAlignment="Top"/>
            </Grid>
            <Rectangle Height="1" Fill="#333333" Margin="0,0,0,10"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="IconText" Text="🏆" FontSize="24" VerticalAlignment="Center" Margin="0,0,15,0"/>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="TitleText" Text="Master of Time" Foreground="#FFFFFF" FontSize="13" FontWeight="Bold" TextWrapping="Wrap"/>
                    <TextBlock x:Name="XpText" Text="+1000 XP" Foreground="#88CC88" FontSize="10" FontWeight="Bold" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
